---
import { site } from '../data/site';
import logoFull from '../assets/img/logo-muccino.jpg';
import logoM from '../assets/img/logo-m.jpg'; // o .png

const waMinorista = `https://wa.me/${site.phoneWa}?text=${encodeURIComponent(site.waMessageMinorista)}`;
const waMayorista = `https://wa.me/${site.phoneWa}?text=${encodeURIComponent(site.waMessageMayorista)}`;
---

<section class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
	<!-- Header -->
	<div class="flex flex-col gap-4">
		<div
			class="inline-flex items-center gap-2 w-fit rounded-full border border-zinc-800 bg-zinc-900/40 px-3 py-1 text-xs text-zinc-200">
			<span class="h-2 w-2 rounded-full bg-emerald-500"></span>
			<span class="font-semibold">Desde {site.since}</span>
			<span class="text-zinc-500">•</span>
			<span class="text-zinc-300">{site.trustLine}</span>
		</div>

		<h1 class="text-3xl sm:text-4xl font-semibold tracking-tight">Nosotros</h1>
		<p class="text-zinc-300 max-w-3xl">
			En <span class="font-semibold text-zinc-100">{site.brand}</span> nos dedicamos
			al
			<span class="font-semibold text-zinc-100">
				fraccionamiento de aceite de soja comestible</span
			>, cuidando cada etapa para ofrecer un producto consistente y una atención
			directa.
		</p>
	</div>

	<!-- Bloques -->
	<div class="mt-10 grid grid-cols-1 lg:grid-cols-2 gap-6">
		<!-- Identidad -->
		<div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6">
			<div class="flex items-center gap-3">
				<img src={logoM.src} alt="Muccino" class="h-10 w-10 rounded-xl" />
				<div>
					<div class="text-sm text-zinc-400">Quiénes somos</div>
					<div class="text-lg font-semibold text-zinc-100">
						Familia, trabajo y constancia
					</div>
				</div>
			</div>

			<p class="mt-4 text-zinc-300">
				Somos una empresa paraguaya con trayectoria, enfocada en brindar un
				producto confiable, con presentaciones pensadas para hogares, despensas
				y gastronomía.
			</p>

			<div class="mt-5 grid grid-cols-1 sm:grid-cols-3 gap-3">
				<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
					<div class="text-2xl font-semibold text-zinc-100">{site.since}</div>
					<div class="text-xs text-zinc-400">Año de inicio</div>
				</div>
				<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
					<div class="text-2xl font-semibold text-zinc-100">PY</div>
					<div class="text-xs text-zinc-400">Empresa paraguaya</div>
				</div>
				<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
					<div class="text-2xl font-semibold text-zinc-100">Soja</div>
					<div class="text-xs text-zinc-400">Aceite comestible</div>
				</div>
			</div>
		</div>

		<!-- Propuesta de valor -->
		<div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6">
			<div class="flex items-center justify-between gap-4">
				<img src={logoFull.src} alt="Muccino" class="h-10 w-auto" />
				<div class="text-xs text-zinc-400 text-right">{site.trustLine}</div>
			</div>

			<div class="mt-5 space-y-4">
				<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
					<div class="font-semibold text-zinc-100">
						Fraccionamiento controlado
					</div>
					<p class="mt-1 text-sm text-zinc-400">
						Buscamos consistencia en el producto y una presentación prolija y
						segura.
					</p>
				</div>

				<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
					<div class="font-semibold text-zinc-100">
						Variedad de presentaciones
					</div>
					<p class="mt-1 text-sm text-zinc-400">
						Tamaños pensados para consumo diario, familiar y uso intensivo
						(gastronomía).
					</p>
				</div>

				<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
					<div class="font-semibold text-zinc-100">
						Atención directa por WhatsApp
					</div>
					<p class="mt-1 text-sm text-zinc-400">
						Consultas rápidas para precio, disponibilidad y pedidos.
					</p>
				</div>
			</div>
		</div>
	</div>

	<!-- Cómo comprar (mini) -->
	<div class="mt-6 rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6">
		<h2 class="text-xl font-semibold">Cómo comprar</h2>
		<p class="mt-2 text-sm text-zinc-400">
			Elegís la presentación y coordinamos por WhatsApp.
		</p>

		<div class="mt-4 grid grid-cols-1 md:grid-cols-3 gap-3 text-sm">
			{
				[
					['1', 'Elegí el tamaño', 'Revisá las presentaciones disponibles.'],
					['2', 'Escribinos', 'Confirmá precio y disponibilidad por WhatsApp.'],
					['3', 'Coordinamos', 'Entrega o retiro según zona y cantidad.'],
				].map(([n, t, d]) => (
					<div class="rounded-xl border border-zinc-800 bg-zinc-950/30 p-4">
						<div class="flex items-center gap-3">
							<div class="h-8 w-8 rounded-full border border-zinc-800 bg-zinc-950/60 flex items-center justify-center font-semibold text-zinc-100">
								{n}
							</div>
							<div class="font-semibold text-zinc-100">{t}</div>
						</div>
						<div class="mt-2 text-zinc-400">{d}</div>
					</div>
				))
			}
		</div>

		<div class="mt-5 flex flex-col sm:flex-row gap-3">
			<a
				href={waMinorista}
				target="_blank"
				rel="noopener"
				class="inline-flex items-center justify-center rounded-xl bg-emerald-600 px-5 py-3 font-semibold hover:bg-emerald-500">
				Consultar (minorista)
			</a>
			<a
				href={waMayorista}
				target="_blank"
				rel="noopener"
				class="inline-flex items-center justify-center rounded-xl border border-amber-400/40 px-5 py-3 font-semibold hover:border-amber-400/70 hover:shadow-[0_0_16px_rgba(251,191,36,0.18)]">
				Pedir catálogo (mayorista)
			</a>
		</div>

		<p class="mt-3 text-xs text-zinc-500">
			WhatsApp: <span class="text-zinc-200 font-semibold"
				>{site.phoneDisplay}</span
			>
		</p>
	</div>

	<!-- CTA final -->
	<div class="mt-10 flex flex-col sm:flex-row gap-3">
		<a
			href="/#presentaciones"
			class="inline-flex items-center justify-center rounded-xl bg-zinc-100 px-5 py-3 font-semibold text-zinc-900 hover:bg-white">
			Ver presentaciones
		</a>
		<a
			href="/#contacto"
			class="inline-flex items-center justify-center rounded-xl border border-zinc-700 px-5 py-3 font-semibold hover:border-zinc-500">
			Contacto
		</a>
	</div>
</section>
